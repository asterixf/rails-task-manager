<h1>Task</h1>
<h2><%= @task.title %></h2>
<p><%= @task.details  %></p>
<p><%= link_to "Edit Task", edit_path %></p>
<p><%= link_to "Delete", task_path(@task), data: {turbo_method: :delete, turbo_confirm: "Are you sure yo want to delete? (delete can not be undone)"} %></p>
<%= link_to "Back to list", tasks_path %>
